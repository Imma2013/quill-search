use_default_settings: true

general:
  instance_name: "Quill search"

search:
  safe_search: 1
  formats:
    - html
    - json

server:
  secret_key: "ultrasecretkey"
  limiter: false
  image_proxy: false
